# Audio capture
pyaudio>=0.2.14

# Voice Activity Detection (lightweight, no ML runtime needed)
webrtcvad>=2.0.10

# Real-time ASR — free, local, true streaming (Pattern 2)
vosk>=0.3.45

# Batch ASR — higher accuracy, auto-downloads model on first use (Pattern 1)
faster-whisper>=1.0.0

# LLM
anthropic>=0.40.0

# Transitive / utility
numpy>=1.24.0
